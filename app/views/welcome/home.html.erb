<%# Add the Facebook login link here %>
<% if session[:user_id] %>
    <h1><%= @user.name %></h1>
    <h2>Email: <%= @user.email %></h2>
    <n2>Facebook UID: <%= @user.uid %></h2>
    <p>
    <img src= "<%= @user.image %>">
    </p>
<% else %>
    <%= link_to('Log in with Facebook!', '/auth/facebook') %>
<% end %>